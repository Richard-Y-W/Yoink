# App repo sync — 2026-07-07

Content originally committed to the Yoink app repo
(`Richard-Y-W/Yoink`, commit `4e15c6f`) that belongs in this art repo.

## What came over

- `style-guide.md` — the canonical Drop Art Pack style guide (chibi 3D
  vinyl-clay renders, 4 families × 5 rarities with mint counts).
- `batches/` — the 8 ready-made generation prompt sheets covering the 57
  non-hero catalog items, plus `CODEX-PROMPT.md` (the runner instructions).
- `renders/` — the first 16 finished batch renders (Pocket Tech batch 01–02
  plus `doodle-sticker-sheet` from Holo Finds), filenames matching the batch
  sheets exactly:

  gummy-game-brick, pixel-pet-cam, minty-mini-console, milky-mouse-ball,
  taffy-tape-deck, puffy-pager-pal, chrome-robo-walkie, jelly-joystick,
  sherbet-boombox, marshmallow-keyboard, glow-dial-rotary, dreamy-disc-player,
  opal-antenna-tv, starlit-synth-keys, prism-projector-pod,
  doodle-sticker-sheet

## Notes

- These renders use the app catalog's plain item ids (no family prefix) —
  the batch sheets in `batches/` reference those exact filenames.
- The item metadata (family, rarity, mint count, price, motif) lives in the
  app repo at `yoink/src/dropCatalog.js`; these 16 items are not yet merged
  into `item-catalog.json` here.
- The app repo keeps its own copies under `yoink/src/assets/drops/` because
  the render pipeline targets that directory (screens will import them once
  the set is wired in).

## Update — batch generator + hero render mapping

- `tools/generate-drops.mjs` — batch generator for the remaining renders.
  It lives in (and runs from) the app repo at `yoink/tools/generate-drops.mjs`
  because it imports `yoink/src/dropCatalog.js`; the copy here is the synced
  reference. It generates every missing render in one unattended run
  (gpt-image-1 or gemini-2.5-flash-image, concurrency, retries, resume) and
  writes a contact-sheet HTML for a single end-of-run style review — replaces
  the batch-by-batch manual flow in `batches/CODEX-PROMPT.md`.
- The 16 original hero renders in `renders/` were copied into the app repo
  under their app catalog ids (same bytes, renamed — not re-uploaded here):
  `pocket-tech-bubble-crt → bubble-crt`, `pocket-tech-jelly-flip-phone →
  jelly-flip-phone`, `pocket-tech-pocket-pixel-mp3 → pocket-pixel-mp3`,
  `pocket-tech-flashpop-toy-camera → flashpop-toy-camera`,
  `holo-finds-frog-foil-card → frog-foil-card`,
  `holo-finds-cosmic-sticker-slab → cosmic-sticker-slab`,
  `holo-finds-lucky-pog-stack → lucky-pog-stack`,
  `holo-finds-glimmer-ticket-relic → glimmer-ticket-relic`,
  `desk-pets-mochi-blob → mochi-blob`,
  `desk-pets-sleepy-star-charm → sleepy-star-charm`,
  `desk-pets-button-eye-sprout → button-eye-sprout`,
  `desk-pets-tiny-desk-dino → tiny-desk-dino`,
  `snack-relics-cereal-prize-rocket → cereal-prize-rocket`,
  `snack-relics-capsule-ghost-toy → capsule-ghost-toy`,
  `snack-relics-vending-ring-pop-relic → vending-ring-pop`,
  `snack-relics-crinkle-pack-mascot → crinkle-pack-mascot`.
- App catalog status: 32 of 73 items rendered (16 heroes + 16 batch);
  41 remaining, pending an image API key for the generator.
